database:
    driver: pdo_mysql
    host: localhost
    db_name: chicherina_db
    db_user: root
    db_password: null

root.setup:
    pattern: '^/root'
    login_path: '/login'
    check_path: '/root/login_check'
    redirect_path: '/root'
    username: 'root'
    role: 'ROLE_ROOT'
    password: 'ctrnjh'
    logout_path: '/root/logout'